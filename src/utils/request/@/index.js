import {
    INTERCEPTOR,
    FORMATBODY,
    FORMATQUERY,
    FORMATFORMDATA
} from './@interceptor';

import {
    OPTIONS,
    CRENDENTIALS,
    METHOD
} from './@options';

import {
    HEADERS,
    AUTHORIZATION
} from './@headers';


export {
    INTERCEPTOR,
    FORMATBODY,
    FORMATQUERY,
    FORMATFORMDATA,

    OPTIONS,
    CRENDENTIALS,
    METHOD,

    HEADERS,
    AUTHORIZATION
};
